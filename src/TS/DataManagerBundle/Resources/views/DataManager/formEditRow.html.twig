{{ form_start(formEditRow) }}
<div class="modal-body">
    {{ form_errors(formEditRow) }}

    {{ form_row(formEditRow.energyInjected) }}
    {{ form_row(formEditRow.irradiation) }}
    {{ form_row(formEditRow.ens) }}

</div>
<div class="modal-footer">
    {{ form_widget(formEditRow.Save, {'attr': {'class': 'btn btn-info'}}) }}
    <button class="btn btn-info" data-dismiss="modal">Cancel</button>
</div>

{{ form_rest(formEditRow) }}

{# Fermeture de la balise <form> du formulaire HTML #}
{{ form_end(formEditRow) }}